import React from 'react';
import Nav from '../Navbar/Nav';
import './Header.css'

const Header = () => {
    return (
        <header>
            <Nav></Nav>
        </header>
    );
};

export default Header;